{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend for the event management system",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test jest --runInBand",
    "test:watch": "cross-env NODE_ENV=test jest --watch --runInBand",
    "test:coverage": "cross-env NODE_ENV=test jest --coverage --runInBand",
    "test:auth": "cross-env NODE_ENV=test jest tests/auth.test.js --runInBand",
    "test:events": "cross-env NODE_ENV=test jest tests/event.test.js --runInBand",
    "test:registrations": "cross-env NODE_ENV=test jest tests/registration.test.js --runInBand"
  },
  "author": "Balaji Sudarshan, Eswar Kumar",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cloudinary": "^2.8.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "express-rate-limit": "^8.2.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.2",
    "multer": "^2.0.2"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^29.7.0",
    "mongodb-memory-server": "^9.5.0",
    "nodemon": "^3.1.10",
    "supertest": "^6.3.4"
  }
}
